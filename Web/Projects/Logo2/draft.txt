1. Взять файлы одного типа из папки (указывается в начале)
2. Склеить их в один файл с указанным в скобках именем
3. Выгрузить эти файлы (libs.min.js и libs.min.css) в папку результата,
    при этом обязательно прописать их в link/script в исходнике html


============
1.Pug
